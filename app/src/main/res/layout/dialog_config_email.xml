<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="#1E293B">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Emails de Destino"
        android:textColor="#FFFFFF"
        android:textSize="14sp"
        android:textStyle="bold"
        android:layout_marginBottom="8dp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Ingresa hasta 10 emails separados por comas"
        android:textColor="#94A3B8"
        android:textSize="12sp"
        android:layout_marginBottom="16dp" />

    <EditText
        android:id="@+id/et_emails_destino"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:hint="email1@empresa.com, email2@empresa.com"
        android:inputType="textEmailAddress|textMultiLine"
        android:background="@drawable/bg_edittext"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="12dp"
        android:gravity="top"
        android:textColor="#FFFFFF"
        android:textColorHint="#64748B" />

    <TextView
        android:id="@+id/tv_contador_emails"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="📧 0 email(s) configurado(s)"
        android:textColor="#94A3B8"
        android:textSize="12sp"
        android:layout_marginTop="8dp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="💡 El reporte se enviará a todos los emails en una sola operación"
        android:textColor="#00F0FF"
        android:textSize="11sp"
        android:layout_marginTop="12dp"
        android:drawablePadding="8dp" />

</LinearLayout>